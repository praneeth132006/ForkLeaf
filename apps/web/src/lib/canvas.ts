/**
 * Canvases live in the markdown engine, so a board can be drawn inside a note
 * by the editor as well as opened on its own. Re-exported here for the app.
 */
export * from "@forkleaf/markdown-engine";
